import React from 'react'

function NoAuthorization() {

    //This page is used when a user with no authorization tries to access a page, the layout won't show these pages but they could be entered through the URL.

  return (
    <div>NoAuthorization</div>
  )
}

export default NoAuthorization